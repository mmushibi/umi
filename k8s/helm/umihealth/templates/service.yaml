apiVersion: v1
kind: Service
metadata:
  name: {{ include "umihealth.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "umihealth.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
